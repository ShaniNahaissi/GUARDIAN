name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Deploy Code
        run: |
          cd /home/cs507/GUARDIAN
          git pull origin main
          sudo docker compose -f docker-compose.yml up --build -d
