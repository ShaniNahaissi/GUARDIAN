# Project instructions

- Invoke the `ponytail` skill (full intensity) at the start of every coding task by default, even without the user saying "ponytail". Stop only if the user says "stop ponytail" / "normal mode".
